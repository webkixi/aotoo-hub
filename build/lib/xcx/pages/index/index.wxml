<view class="boxer">
  <scroll-view scroll-y="{{isScrollx}}" class="scroll-wrap-nav">
    <ui-item item="{{headLogo}}" />
    <ui-tree list="{{menuData}}" />
  </scroll-view>
  <ui-list list="{{nav}}" />
  <ui-item item="{{tipsX}}" />
</view>

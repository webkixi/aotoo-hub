export default function(Pager) {
  const {lib} = ao2
  const {nav} = Pager
  function template(state, props) {
    return (
      <div className="bgurl">
        123
      </div>
    )
  }
  
  return Pager({
    template,

    data: {},
    
    onLoad(options){
    },

    onUnload(){

    },

    onReady(){
    }
  })
}
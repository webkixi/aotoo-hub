<wxs src="../utils.wxs" module="utils" />
<template name="text">
  <text 
    class="{{item.class}}"
    style="{{item.style}}"
    selectable="{{item.selectable||false}}"
    space="{{item.space||false}}"
    decode="{{item.decode||false}}"
  >
  {{item.title}}
  </text>
</template>